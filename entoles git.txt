 ->mkdir:δημιουργία φακέλου
 ->cd(change directory):για να μπούμε στον φάκελο
 ->git int : αρχικοποίηση αποθηκευτικού χώρου (repository)
 ->ls : λίστα των πραγμάτων του repository
 ->touch:δημιουργία αρχείου στο repository
 ->git add : ονομασία αρχείου : προσθέτει το  αρχείο στο repository
 ->git add : θα προσθέσει όλα τα αρχεία του φακέλου η των υποφακέλων αυτού του φακέλου 
 ->git commit:για να δεσμεύσω τις αλλαγές (πχ οταν κάνω add).το -m".."για να βάλω μήνυμα που θα είναι ορατό στο κώδικα μόνο